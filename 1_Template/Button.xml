<Ui xmlns="http://www.blizzard.com/wow/ui/" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.blizzard.com/wow/ui/
https://raw.githubusercontent.com/Gethe/wow-ui-source/live/Interface/AddOns/Blizzard_SharedXML/UI.xsd">

    <Button name="WoWToolsButtonTemplate" virtual="true">
        <Size x="23" y="23"/>
        <PushedTexture atlas="PetList-ButtonSelect"/>
        <HighlightTexture atlas="PetList-ButtonHighlight"/>
        <Scripts>
            <OnLoad>
                self:EnableMouseWheel(true)
                self:RegisterForClicks(WoWTools_DataMixin.LeftButtonDown, WoWTools_DataMixin.RightButtonDown)
            </OnLoad>
            <OnLeave>
                GameTooltip_Hide()
                if self.set_alpha then
                    self:set_alpha()
                end
            </OnLeave>
            <OnEnter>
                if self.tooltip then
                    GameTooltip:SetOwner(self, 'ANCHOR_LEFT')
                    GameTooltip:ClearLines()
                    if type(self.tooltip)=='function' then
                        self:tooltip(GameTooltip)
                    else
                        GameTooltip:AddLine(self.tooltip)
                    end
                    GameTooltip:Show()
                end
                if self.set_alpha then
                    self:set_alpha()
                end
            </OnEnter>
        </Scripts>
    </Button>

    <DropdownButton name="WoWToolsMenuButtonTemplate" virtual="true">
        <Size x="23" y="23"/>
        <NormalTexture atlas="ui-questtrackerbutton-filter"/>
        <PushedTexture atlas="ui-questtrackerbutton-filter-pressed"/>
        <HighlightTexture atlas="ui-questtrackerbutton-red-highlight"/>
        <Scripts>
            <OnLoad>
                self:EnableMouseWheel(true)
                self:RegisterForMouse("RightButtonDown", 'LeftButtonDown', "LeftButtonUp", 'RightButtonUp')
                WoWTools_TextureMixin:SetButton(self, {alpha=0.5})
            </OnLoad>
            <OnLeave>
                GameTooltip_Hide()
                if self.set_alpha then
                    self:set_alpha()
                end
            </OnLeave>
            <OnEnter>
                if self.tooltip then
                    GameTooltip:SetOwner(self, 'ANCHOR_LEFT')
                    GameTooltip:ClearLines()
                    if type(self.tooltip)=='function' then
                        self:tooltip(GameTooltip)
                    else
                        GameTooltip:AddLine(self.tooltip)
                    end
                    GameTooltip:Show()
                end
                if self.set_alpha then
                    self:set_alpha()
                end
            </OnEnter>
        </Scripts>
    </DropdownButton>

</Ui>